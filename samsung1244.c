#include <stdio.h>

int main(){
    int TC;
    scanf("%d", &TC);
    int num[7];
    int rotate;
    int j;
    while (TC != 0){
        TC--;
        for (int i = 0 ; num[i] == '\0' ; i++){
            scanf("%c", & num[i]);
            j = i;
        }
        scanf("%d", &rotate);
        for (int i = 0 ; i < rotate ; i++){
            for( ; j >= 0 ; j--){

                
            }
        }

    }

}